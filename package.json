{
  "name": "paxthem-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Worker HTTP + WebSocket Paxthem",
  "main": "worker.js",
  "type": "commonjs",
  "scripts": {
    "start": "node worker.js",
    "check": "node --check worker.js && node --check db.js && node --check normalizer.js && node --check paxthem-http.js && node --check paxthem-ws.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "mysql2": "^3.14.0",
    "ws": "^8.18.0"
  }
}
